<h1>Listing instrument_requirements</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @instrument_requirements.each do |instrument_requirement| %>
  <tr>
    <td><%= link_to 'Show', instrument_requirement %></td>
    <td><%= link_to 'Edit', edit_instrument_requirement_path(instrument_requirement) %></td>
    <td><%= link_to 'Destroy', instrument_requirement, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Instrument requirement', new_instrument_requirement_path %>
