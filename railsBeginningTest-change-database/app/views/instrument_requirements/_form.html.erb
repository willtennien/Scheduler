<%= form_for(@instrument_requirement) do |f| %>
  <% if @instrument_requirement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instrument_requirement.errors.count, "error") %> prohibited this instrument_requirement from being saved:</h2>

      <ul>
      <% @instrument_requirement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
